name: Build-Lint-Test
on: [workflow_dispatch]
jobs:
  lint:
    runs-on: ubuntu-20.04
    steps:
      - name: Get rep code
        uses: actions/checkout@v4
      - name: Install deps
        run: npm ci
